import { DeclarationListEmitMode } from "@angular/compiler";

export interface ListaofertasI{
    ofertaId: BigInteger;
    Description: string;
    Deadline: Date;
    AddPoints: BigInteger;
    SubPoints: BigInteger;
}